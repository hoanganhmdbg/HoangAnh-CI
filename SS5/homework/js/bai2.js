'getElementById() chỉ có thể sử dụng để lấy các phần tử bằng ID của chúng cái này sẽ luôn trả về 1 phần tử duy nhất vì Id là duy nhâts';
'querySelector() nó sẽ trả về bất kể phần tử nào dù là id hay class hay tag tuy nhiên nó sẽz chỉ trả về phần tử đầu tiên nó tìm thấy';
'querySelectorAll() sẽ trả về tất cả các phần tử mà khớp với tham số truyền vào là lưu dưới dạng NodeList()';
'getElementsByClassName() and getElementsByTagName() thì tương tự nhau nó sẽ trả về 1 danh sách đc lưu vào HTMLCollection';
